var config = module.exports = {};

// Bot config
config.bot = {
	nick:		'nick',
	userName:	'user name',
	realName:	'real name'
};

// Server config
config.server = {
	address:	'host.address',
	port:		6667,
	channels:	[ '#channel' ]
};

// Commands config
config.prefix =	'!';
